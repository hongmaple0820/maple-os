# MapleOS Baseline 报告 — 2026-06-20

本文件由 Track 0 / T0-1 产出，记录在干净环境复现仓库可构建、可测试状态所需的全部信息。后续每个 PR 必须在不退化本 baseline 的前提下推进。

## 1. 环境信息

| 项目 | 版本 |
| --- | --- |
| OS | Linux x86_64 (容器) |
| Rust toolchain | rustc 1.96.0 (ac68faa20 2026-05-25) / cargo 1.96.0 |
| Node.js | v24.16.0 |
| pnpm | 11.8.0（仓库 `packageManager` 字段指定 11.3.0，可用） |
| Playwright | 1.60.0 + chromium-headless-shell 148.0.7778.96 |
| Cargo target dir | `/tmp/my-project/cargo-target`（避免根分区空间不足） |
| Cargo home | `/tmp/my-project/cargo-home`（registry 软链接到 `~/.cargo/registry` 复用缓存） |

## 2. 工具链准备命令

```bash
# Rust
curl -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain stable --profile minimal
. "$HOME/.cargo/env"

# pnpm
npm install -g pnpm

# 把 cargo target 移到更大的磁盘（PolarFS，29PB 可用）
export CARGO_TARGET_DIR=/tmp/my-project/cargo-target
export CARGO_HOME=/tmp/my-project/cargo-home
mkdir -p $CARGO_TARGET_DIR $CARGO_HOME
ln -s /home/z/.cargo/registry $CARGO_HOME/registry

# Playwright 浏览器
pnpm exec playwright install chromium
```

## 3. 仓库状态

| 项目 | 值 |
| --- | --- |
| 分支 | `feat/closure-track0-prep`（基于 `master` `774d0eb`） |
| 工作区 | 干净（仅本报告与若干修复提交） |

## 4. Baseline 验证结果

### 4.1 cargo check

```bash
. "$HOME/.cargo/env"
export CARGO_TARGET_DIR=/tmp/my-project/cargo-target
export CARGO_HOME=/tmp/my-project/cargo-home
cargo check --workspace --all-targets
```

**结果**：通过。仅 5 个 warning（dead_code / unused_imports，均为非阻塞）。

> **修复**：初始 baseline 有 3 个编译错误在 `server/src/v3_auth.rs` 测试模块（`Parts::default()` 在 axum 0.7 不存在）。已通过新增 `make_test_parts()` helper 修复。详见 commit `fix(v3_auth): replace Parts::default() with helper for axum 0.7`。

### 4.2 cargo test --lib（单元测试）

```bash
cargo test --workspace --lib
```

**结果**：全绿，440 个测试通过。

| Crate | 测试数 |
| --- | --- |
| maple-agent | 285 |
| maple-engine | 41 |
| maple-llm | 69 |
| maple-kb | 14 |
| maple-collab | 13 |
| maple-sync | 5 |
| mapleos-server (lib) | 4 |
| maple-macro / maple-rpc | 0 + 9 |
| **合计** | **440** |

### 4.3 v3 API 集成测试

```bash
export DATABASE_URL="sqlite::memory:"
/tmp/my-project/cargo-target/debug/deps/v3_api_integration-d2423178e7b09f67 --test-threads=1
```

**结果**：12/12 全绿。

```
test test_agent_hooks_crud ... ok
test test_approval_workflow ... ok
test test_create_and_list_groups ... ok
test test_cron_jobs ... ok
test test_dm_workflow ... ok
test test_group_members ... ok
test test_group_rules_crud ... ok
test test_memory_store_and_search ... ok
test test_message_attachments_lifecycle ... ok
test test_send_and_list_messages ... ok
test test_task_lifecycle ... ok
test test_workflow_definitions_and_runs ... ok
```

### 4.4 server integration tests（`server/tests/integration.rs`）

3 个测试都是元测试（递归调用 `cargo check` / `cargo test`），本环境因为编译耗时长 + 子进程超时控制问题未跑完。

**结论**：本测试设计需要优化，纳入 Track 0-4 范围（拆 main.rs 时一并改造）。功能上等价于 4.1 + 4.2 已经验证过的事情。

### 4.5 pnpm build（Web 前端）

```bash
pnpm install --no-frozen-lockfile
pnpm --filter mapleos-web build
```

**结果**：通过。

```
Route (app)                                 Size  First Load JS
┌ ○ /                                    86.2 kB         253 kB
├ ○ /_not-found                            990 B         103 kB
├ ○ /collaboration                         681 B         161 kB
└ ○ /v3                                  13.6 kB         123 kB
```

### 4.6 Playwright E2E product-gate

```bash
export CARGO_TARGET_DIR=/tmp/my-project/cargo-target
export CARGO_HOME=/tmp/my-project/cargo-home
export PATH="/home/z/.cargo/bin:$PATH"
pnpm test:e2e:product
```

**结果**：1/1 通过（21.4s）。

```
✓  1 [chromium] › tests/e2e/product-gate.spec.ts:11:7
   › Product Gate › local mode boots and core product loop stays usable (4.2s)

  1 passed (21.4s)
```

> **修复 1**：测试选择器 strict mode 违规——`getByText('E2E Agent')` 同时匹配 toast 和卡片。改为 `getByRole('button', { name: /E2E Agent/ })`。
>
> **修复 2**：`scripts/qa/start-e2e-backend.mjs` 原本硬编码 `cargoTargetDir` 为 `.tmp/cargo-target-e2e`，会强制重新编译。改为读取 `CARGO_TARGET_DIR` 环境变量以复用现有缓存。详见 commit `fix(e2e): allow CARGO_TARGET_DIR override + tighten agent assertion`。

## 5. 已知 baseline 问题（不阻塞，但需在后续 Track 修）

| 问题 | 影响 | 处理 Track |
| --- | --- | --- |
| `server/src/main.rs` 5654 行单文件 | 后端开发体验差，路由+handler 全塞一起 | Track 0-4 |
| `server/tests/integration.rs` 递归调用 cargo，CI 跑不完 | 集成测试形同虚设 | Track 0-4 |
| 5 个 cargo warning（dead_code: `extract_token`、`verify_and_extract`、`role` 字段等） | 代码卫生问题 | Track 0-4 |
| pnpm WARN: `pnpm` 字段不再被读取 | 配置过时 | Track 0-4 |
| `product-gate.spec.ts` 只覆盖 smoke 路径 | E2E 形同虚设 | Track 4-1 |
| `apps/web/src/components/workflow-manager.tsx` 856 行 | 前端单体 | Track 4-8 |
| `apps/web/src/components/settings-page.tsx` 496 行 | 前端单体 | Track 4-8 |
| `migrations/` 无 `execution_events` / `tool_invocations` 表 | 事实链基础设施缺 | Track 0-3 |
| `list_models()` 返回 `Vec<String>` 与前端期望类型不符 | #86 bug | Track 3-1 |

## 6. CI 命令清单（PR 必须全部通过）

```bash
# 1. Rust 编译检查
. "$HOME/.cargo/env"
export CARGO_TARGET_DIR=/tmp/my-project/cargo-target
export CARGO_HOME=/tmp/my-project/cargo-home
cargo check --workspace --all-targets

# 2. Rust 单元测试
cargo test --workspace --lib

# 3. 前端构建
pnpm install --no-frozen-lockfile
pnpm --filter mapleos-web build

# 4. E2E product-gate
pnpm exec playwright install chromium
export PATH="/home/z/.cargo/bin:$PATH"
pnpm test:e2e:product
```

## 7. 后续 PR 验收基线

任何 PR 合并到 `master` / `feat/closure-track*` 前必须：

- [ ] `cargo check --workspace --all-targets` 无 error
- [ ] `cargo test --workspace --lib` 不退化（pass 数 ≥ 440）
- [ ] `pnpm --filter mapleos-web build` 成功
- [ ] `pnpm test:e2e:product` 1/1 通过
- [ ] PR 描述包含 user entry / runtime path / persistence path / error path / 验证证据

---

**生成时间**：2026-06-20 23:50 UTC+8  
**生成者**：Track 0 / T0-1  
**仓库提交**：`master` 之上的 `feat/closure-track0-prep` 分支
